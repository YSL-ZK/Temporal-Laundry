# Security Memory
> Auto-generated by security-skill v1.0.0
> Do not delete — used by the AI to track security state
> Last updated: never

## Project Profile
- detected_stacks: [Next.js 16, React 19, TypeScript, pnpm]
- deployment_platforms: [Vercel]
- databases: [Supabase Postgres]
- has_ai_features: true
- compliance_mode: none  # none | gdpr | hipaa | pci-dss | soc2
- environment: prod   # dev | staging | prod

## User Preferences
- ask_before_modify: true        # Always ask before modifying existing files
- ask_before_design_change: true # Always ask if visual/UI might be affected
- language: auto                 # auto | fr | en | es | de
- report_format: file            # file | chat
- rotation_reminder_days: 90     # Remind when secrets older than N days

## Security Score History
| Date | Score | Critical | High | Medium | Notes |
|------|-------|----------|------|--------|-------|
| Never run | -/100 | - | - | - | No audit run yet |

## Accepted Risks
# Risks you have reviewed and accepted. Add entries below.
# Format:
# - risk: description
#   reason: why you accept it
#   accepted_on: YYYY-MM-DD
accepted_risks: []

## Never Ask Again
# Rule IDs to skip confirmation for (already reviewed)
never_ask_again: []

## Custom Rules
# Override default behavior. Examples:
# - IGNORE: CORS https://partner.com (known partner API)
# - IGNORE: PORT 5432 (internal DB, expected)
custom_rules: []

## Secrets Lifecycle
# Track when secrets were last rotated
# Format:
# SECRET_NAME:
#   created: YYYY-MM-DD
#   last_rotated: YYYY-MM-DD
#   rotation_policy: 90days
#   status: ok | due | overdue
secrets: {}

## Incident History
- none

## Security History Addendum
- 2026-08-27: Added a versioned migration to revoke `PUBLIC`, `anon`, and `authenticated` execution of a hosted `public.rls_auto_enable()` event-trigger function when present.
- 2026-08-27: Moved household RLS helper functions into the non-exposed `private` schema, pinned their search paths, granted only authenticated policy evaluation access, and rewired all dependent policies.
- Verification pending: link the hosted Supabase project, run `pnpm exec supabase db push --linked`, then rerun Security Advisor and the function-privilege query.
- 2026-08-27: Read-only security audit completed for Next.js, Supabase, and pnpm. No tracked secrets found; report written to `security-report.md`. High-priority remediation remains for the vulnerable PostCSS dependency and production security headers.
- 2026-08-27: Runtime hardening applied: zero-vulnerability pnpm audit after PostCSS 8.5.26 override, security headers verified locally, fixed callback origin, stronger local Auth defaults, and reduced account-enumeration leakage. Remaining work is hosted Supabase configuration and a nonce-based CSP refactor.
- 2026-08-27: Upgraded to Next.js 16.3.3 and React 19.2.8. Replaced root `middleware.ts` with `proxy.ts`, removed the obsolete `instant` route option, and migrated the lint script to ESLint. Lint and TypeScript checks pass; pnpm audit reports zero known vulnerabilities.
- 2026-08-27: Pnpm workspace policy permits only the required `esbuild` postinstall and explicitly denies the optional `unrs-resolver` build step.
- 2026-08-27: Applied all five finance, auth, RLS, ledger, invitation, receipt, and RPC-hardening migrations to linked project ilemvtovlpjaawczfjfd. Security Advisor confirms anonymous privileged-RPC access is revoked; four authenticated-only finance RPC warnings remain intentional and validate auth/household access internally.
- 2026-08-27: Applied migration 20260827223000 to move the four finance mutation RPCs behind service-role-only execution. Server actions now verify the signed-in user before passing the actor ID to database functions, which re-check profile, household, account, and invitation access. Browser roles cannot write ledger, transaction-item, transaction-tag, or invitation rows directly. Remote database lint, ESLint, TypeScript, and production build pass; rerun the hosted Security Advisor to refresh its cached findings.
- 2026-08-28: Added a service-role-only shopping checkout function. It re-checks actor/list/account/category access, rate-limits checkout attempts, calculates line and list adjustments server-side, posts a balanced expense atomically, and retains transaction line items while removing only purchased shopping-list rows. Supabase database lint passes.
- 2026-08-28: Added service-role-only plan workflow functions for recurring confirmation, goal allocation, debt payment, and budget envelopes. Each locks the target record, re-checks household and private/shared access, and posts any ledger change atomically. Remote schema lint passes. The only current Security Advisor warning is leaked-password protection, which Supabase documents as unavailable on the Free plan; the Performance Advisor reports an existing duplicate-permissive-policy optimization for household membership reads.
- 2026-08-28: Added an explicit authenticated sign-out control using the browser-safe Supabase client with local-session scope. UI review found no new secret exposure, raw HTML/code execution, or preview-route bypass; lint, TypeScript, unit tests, and the production build pass.
- 2026-08-28: Added a deliberately narrow PWA service worker. It never caches navigation responses or household/API data, only same-origin versioned static assets and a non-sensitive offline page; registration requires a production secure context.
- 2026-08-28: Added an authenticated, RLS-scoped transaction CSV export with a 5,000-row bound, private/no-store response headers, fixed filenames, and spreadsheet-formula injection protection. Regression tests cover escaping. Dependency audit reports zero known vulnerabilities.
- 2026-08-28: Added the read-only Laundry Guide finance assistant. Model credentials remain server-only; RLS-authorized context is aggregated and excludes raw transaction descriptions, account/household names, payees, emails, notes, and receipts. The model has no tools or write path. Same-origin, body/history/output limits, finance-scope screening, timeout handling, free-model allowlisting, and atomic database-backed user/household/project quotas limit prompt-injection, account farming, and cost abuse. Laundry stores usage metadata only, not prompts or answers.
- 2026-08-28: Applied migrations 20260828093746 and 20260828095339 to the linked Supabase project. Browser roles have explicit table/function revocations; service role alone reserves and completes quota events, and a trigger atomically enforces the project-wide daily ceiling. Hosted Security Advisor reports no new AI finding (the Free-plan leaked-password warning remains), and Performance Advisor still reports the pre-existing duplicate household-membership SELECT policies.
- 2026-08-28: Applied migration 20260828100738 for persistent English/Spanish preferences and creator-only expense voiding. The void RPC is service-role-only, authenticates the actor again in PostgreSQL, locks and permits only the creator's active posted expense, rate-limits attempts, and retains an audit event instead of deleting ledger history. Active balances, reports, budget calculations, dashboard lists, and CSV exports exclude voided entries. Hosted function privileges were verified for anon/authenticated/service_role; Security Advisor still reports only the Free-plan leaked-password warning.
- 2026-08-28: Applied migrations 20260828165639 and 20260828171454 for household payees, tags, transaction assignment, and parameterized ledger search. Browser roles have read-only RLS access to organizer records; rate-limited service-only RPCs create payees/tags and atomically post their assignments with audit events. Search uses a SECURITY INVOKER parameterized function so RLS remains active and user text is never interpolated into a PostgREST filter. The follow-up migration qualified all audit rate-limit columns, including creator-only expense voiding; linked Supabase database lint reports no schema errors.
- 2026-08-28: Replaced oversized mobile OS category selection with an accessible Laundry choice dialog and progressively customized all remaining native selects. Custom triggers submit ordinary client-controlled values only; existing Zod validation and service-only/RLS database checks remain authoritative. Dialogs use native modal behavior, keyboard navigation, Escape dismissal, visible selection, and bounded scrolling without adding HTML injection or privileged browser APIs.
